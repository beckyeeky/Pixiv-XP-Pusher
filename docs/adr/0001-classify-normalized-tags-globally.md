# Classify normalized tags globally

Each Normalized Tag receives at most one global Tag Category. Work-level translations and common co-occurring tags may provide classification evidence, but they do not create per-work categories; genuinely ambiguous tags remain unresolved for human review. This trades some contextual precision for a stable classification that can be reused consistently by profile construction, retrieval, and ranking instead of introducing occurrence-level classification throughout the recommendation pipeline.
